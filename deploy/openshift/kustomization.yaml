---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: expanso-system

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml

commonLabels:
  app.kubernetes.io/managed-by: expanso
  app.kubernetes.io/version: "1.0.0"

# Uncomment to customize for your environment
# patches:
#   - patch: |-
#       - op: replace
#         path: /data/OTLP_ENDPOINT
#         value: "https://your-otlp-endpoint.example.com:4318"
#     target:
#       kind: ConfigMap
#       name: expanso-oran-pipeline
